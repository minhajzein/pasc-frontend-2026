# Backend API base URL (no trailing slash).
# Required in production: set this to your deployed backend (e.g. https://your-api.railway.app).
# Leave unset for same-origin requests when the API is proxied/rewritten on the same host.
NEXT_PUBLIC_API_URL=http://localhost:3001
